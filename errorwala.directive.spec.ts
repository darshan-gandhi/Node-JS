import { ErrorwalaDirective } from './errorwala.directive';

describe('ErrorwalaDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorwalaDirective();
    expect(directive).toBeTruthy();
  });
});
